---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: prometheus-metrics-app
  name: prometheus-metrics-app
spec:
  port:
    targetPort: web
  to:
    kind: Service
    name: prometheus-metrics-app
    weight: 100
  wildcardPolicy: None